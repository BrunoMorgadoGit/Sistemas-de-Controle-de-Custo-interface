# 1. Remover pasta de migrações antiga
Remove-Item -Recurse -Force migrations

# 2. Reinicializar Alembic
flask db init

# 3. Gerar migração inicial (detecta todo o schema)
flask db migrate -m "Migração inicial PostgreSQL"

# 4. Aplicar migração no banco PostgreSQL
flask db upgrade
